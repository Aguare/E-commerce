class Alert {
  constructor(title, message, type, show, code) {
    this.title = title;
    this.message = message;
    this.type = type;
    this.show = show;
    this.code = code;
  }
}

export default Alert;
